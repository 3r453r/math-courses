# AI Provider API Keys (optional - can also be set in the browser UI)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_AI_API_KEY=

# Database URL (default: SQLite file in prisma directory)
DATABASE_URL="file:./dev.db"

# Auth.js — required for authentication
AUTH_SECRET=          # Generate with: openssl rand -base64 32

# OAuth providers (configure at least one for production)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# Dev bypass (set to "true" to skip auth in development/testing)
# AUTH_DEV_BYPASS=true

# Turso (libsql cloud) — required for production/Vercel deployment
# When set, DATABASE_URL is ignored and Turso is used instead
# TURSO_DATABASE_URL=libsql://your-db-name-your-org.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# Stripe (payment integration)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_ID=price_...

# Discord community invite link (shown on dashboard, setup, pricing pages)
# NEXT_PUBLIC_DISCORD_INVITE_URL=https://discord.gg/YOUR_INVITE
